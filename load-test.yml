config:
  target: "http://localhost:8082"
  phases:
    - duration: 60                     
      arrivalRate: 100                  
scenarios:
  - flow:
    - get:
        url: "/student/fetch/topics"